module.exports = {
    db: {
        uri: '<REPLACE>', //place the URI of your mongo database here.
    },
    JSONSecret: 'InternshipSecret', //Key used to encode passwords. Do not change after launch or all passwords will be wrong!
    listingPrice: 2000, //Listing price (in pennies)
    stripeSecret: '<REPLACE>' 
};
